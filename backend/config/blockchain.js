// config/blockchain.js

module.exports = {
  // URL of the Ethereum or testnet node (e.g., Ganache, Infura)
  RPC_URL: "https://rpc-mumbai.maticvigil.com", // Example: Polygon Mumbai testnet
  // Private key of the account that will send transactions
  PRIVATE_KEY: "YOUR_PRIVATE_KEY_HERE",
  // Smart contract address (after deployment)
  CONTRACT_ADDRESS: "YOUR_SMART_CONTRACT_ADDRESS_HERE",
  // Optional: blockchain network name
  NETWORK: "mumbai" // or "ropsten", "rinkeby", etc.
};
